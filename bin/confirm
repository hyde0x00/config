#!/usr/bin/env bash

if [ -n "$1" ]; then
  msg="$*"
else
  msg="Confirm"
fi

while read -r -p "$msg [y/n] " input; do
  case "${input,,}" in
    y|yes) exit 0 ;;
    n|no) exit 1 ;;
    *) continue ;;
  esac
done
