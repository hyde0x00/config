#!/usr/bin/env bash

if [ "$(uname)" == "Darwin" ]; then
  if [ -z "$(which gawk 2>/dev/null)" ]; then
    echo "GNU awk is not installed. Installing..."
    brew install gawk
  fi
  gawk "$@"
else
  /usr/bin/awk "$@"
fi
